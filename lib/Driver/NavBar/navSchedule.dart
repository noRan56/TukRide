import 'package:flutter/material.dart';

import 'package:tuk_ride/Maps/Schedule_Ride.dart';

class ScheduleRideScreenNav extends StatelessWidget {
  const ScheduleRideScreenNav({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ScheduleRideScreen();
  }
}
